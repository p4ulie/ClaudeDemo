[project]
name = "polybot"
version = "0.1.0"
description = "Autonomous Polymarket trading bot with AI-powered probability estimation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
