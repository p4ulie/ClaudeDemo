# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

LearnTracker — part of the ClaudeDemo monorepo (`C:\Users\pavol\git\ClaudeDemo`). This is a new project; no code has been written yet.

## Repository Context

The parent repo (`ClaudeDemo`) also contains `test_01/`, a Signal Messenger command bot (Python stdlib only). LearnTracker is a separate project within the same repo.

## Workflow

- Research before planning, plan before coding
- Run tests after every change
- Update documentation when behaviour changes
- Commit and push after tests pass
- License: GPL-3.0 (inherited from parent repo)
